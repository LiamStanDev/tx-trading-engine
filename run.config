# 此文件會被 run.sh 自動載入

# 預設建置類型
# DEFAULT_BUILD_TYPE="debug"

# Benchmark 預設建置類型
# BENCH_BUILD_TYPE="release"

# CMake Generator（預設: Ninja）
# CMAKE_GENERATOR="Ninja"

# 額外的 CMake 參數
# CMAKE_EXTRA_ARGS="-DTX_ENABLE_ASAN=ON"

# 日誌級別 (0=DEBUG, 1=INFO, 2=WARN, 3=ERROR)
# LOG_CURRENT_LEVEL=1
