# 此文件會被 run.sh 自動載入

# =========================
# 建置類型
# =========================
# 預設建置類型（build/test 預設使用）
DEFAULT_BUILD_TYPE="debug"
# Benchmark 預設建置類型
BENCH_BUILD_TYPE="release"
# 有效的建置類型
VALID_BUILD_TYPES="debug release relwithdebinfo minsizerel"

# =========================
# CMake 配置
# =========================
# CMake Generator（推薦: Ninja，回退: Unix Makefiles）
CMAKE_GENERATOR="Ninja"
# 額外的 CMake 參數（全局生效）
# 範例:
#   CMAKE_EXTRA_ARGS="-DTX_ENABLE_ASAN=ON -DTX_ENABLE_TSAN=OFF"
#   CMAKE_EXTRA_ARGS="-DTX_WARNINGS_AS_ERRORS=OFF"
CMAKE_EXTRA_ARGS=""

# =========================
# 目錄配置
# =========================
# 建置目錄前綴（預設: build）
BUILD_DIR_PREFIX="build"
# 安裝目錄前綴（預設: install）
INSTALL_PREFIX="install"

# =========================
# 日誌級別
# =========================
# 日誌級別 (0=DEBUG, 1=INFO, 2=WARN, 3=ERROR)
LOG_CURRENT_LEVEL=1

# =========================
# 工具配置
# =========================
# 啟用 ccache（加速重複編譯）
# TX_ENABLE_CCACHE=ON
# 格式化工具
# CLANG_FORMAT="clang-format-17"
# 靜態分析工具
# CLANG_TIDY="clang-tidy-17"

# =========================
# Sanitizers 配置（僅用於覆寫 CMake 預設值）
# =========================
# 注意：這些選項會覆寫 CMakeLists.txt 中的自動判斷邏輯
# 
# 啟用 Address Sanitizer
# CMAKE_EXTRA_ARGS="${CMAKE_EXTRA_ARGS} -DTX_ENABLE_ASAN=ON"
#
# 啟用 Thread Sanitizer（與 ASan 互斥）
# CMAKE_EXTRA_ARGS="${CMAKE_EXTRA_ARGS} -DTX_ENABLE_TSAN=ON -DTX_ENABLE_ASAN=OFF"
#
# 啟用 Undefined Behavior Sanitizer
# CMAKE_EXTRA_ARGS="${CMAKE_EXTRA_ARGS} -DTX_ENABLE_UBSAN=ON"

# =========================
# 效能優化
# =========================
# 啟用 Link Time Optimization（Release 建議開啟）
# CMAKE_EXTRA_ARGS="${CMAKE_EXTRA_ARGS} -DTX_ENABLE_LTO=ON"
# CPU 架構優化（預設: -march=native）
# 如果需要跨機器相容，改用：
# CMAKE_EXTRA_ARGS="${CMAKE_EXTRA_ARGS} -DCMAKE_CXX_FLAGS=-march=x86-64-v3"
